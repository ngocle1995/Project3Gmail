import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project3Gmail',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
